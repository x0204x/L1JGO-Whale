# 下一版變更摘要
- 收斂玩家面對面互動目標搜尋：交易 `findFaceToFace()` 改用同 ShowID viewer，求婚 `findNearbyPlayer()` 改共用 yiwei `FaceToFace` 語義，要求同 ShowID、面前一格且對方面向自己，避免跨副本交易/求婚或求婚方向顛倒。
- 收斂血盟系統可見性 ShowID 邊界：`ClanSystem` 新增 `clanVisiblePlayers()`，建盟/退盟/解散/踢人/改稱號等附近角色狀態廣播與加入血盟面對面搜尋改用同 ShowID viewer，對齊 yiwei `World.getVisiblePlayer` / `broadcastPacketAll`，避免不同副本收到稱號、血盟名稱或加入血盟 Y/N。
- 收斂王者加護特效 ShowID 邊界：`applyBraveAvatar()` 的 9009 特效改用同 ShowID viewer，對齊 yiwei `pc.sendPacketsAll(new S_SkillSound(pc.getId(), 9009))`，避免不同副本玩家看到王者加護套用特效。
- 收斂傳送技能 ShowID 邊界：`TELEPORT(5)` 出發 169 特效與 `MASS_TELEPORT(69)` 附近血盟成員收集改用同 ShowID viewer，對齊 yiwei `sendPacketsAll` / `World.getVisiblePlayer`，避免跨副本看到傳送特效或被集體傳送詢問。
- 收斂王族目標技能施法動作 ShowID 邊界：`CALL_CLAN(116)` / `RUN_CLAN(118)` 改走共用 `clanSkillViewers()`，施法動作只送施法者與同 ShowID viewer，保留 729 確認、RUN_CLAN 地圖拒絕與傳送語義。
- 收斂 NPC 一般攻擊位置判定：`npcCanAttackPosition()` 改成 yiwei `isAttackPosition()` 語義，普通 NPC 近戰/遠攻必須同 ShowID、在 `ranged` 距離內且通過 LOS；mobskill type 1 仍保留自己的 `range` 判定，避免怪物攻擊入口被直接呼叫時隔空打人。
- 對齊 yiwei 玩家光源更新廣播語義：`UpdatePlayerLight()` 改為自己一定收到 `S_Light`，隱身玩家不對外廣播，非隱身時只送同 ShowID viewer，避免日光術/光源狀態跨副本顯示或暴露隱身玩家。
- 對齊 yiwei 斷線 companion 清理 ShowID 邊界：`InputSystem.cleanupCompanions()` 移除召喚物、娃娃、祭司、寵物、follower 與 follower 還原原 NPC 顯示時改用 companion 自身 ShowID viewer，避免玩家斷線時把同伴移除或還原 NPC 封包漏給不同副本玩家。
- 對齊 yiwei 角色視覺更新廣播語義：`handler.BroadcastVisualUpdate()` 改用同 ShowID viewer 清單並只回送自己一次，避免裝備/變身/勇水等角色外觀更新封包漏給不同副本玩家。
- 收斂物品使用視覺封包 ShowID 邊界：技能書學習特效、傳送卷軸出發 169 特效、藥水加速/勇敢封包與通用物品特效改走 `itemUseViewers()`，魔杖動作也改用同 ShowID viewer，對齊 yiwei 玩家 `sendPacketsAll` / `sendPacketsX*` 的副本隔離。
- 新增物品使用 ShowID 回歸測試：鎖定加速藥水與指定傳送卷軸出發特效不會送給不同 ShowID 玩家。
- 收斂地面效果 ShowID 邊界：`GroundEffect` 補上 `ShowID`，玩家與 NPC 生成的生命之泉、火牢、立方、衝暈效果 NPC、雷霆抓取、寒冰噴吐與墓碑效果會繼承 caster / 玩家所在場景；可視、進場重建、回血、傷害、移除、重複效果檢查都改用同 ShowID 地面效果查詢。
- 新增地面效果 ShowID 回歸測試：鎖定 `GetNearbyGroundEffectsInShow`、`HasGroundEffectAtInShow`、技能生成繼承 ShowID、生命之泉回血、火牢傷害與 VisibilitySystem 不會跨 ShowID 作用或送封包。
- 收斂地上物 ShowID 邊界：`GroundItem` 補上 `ShowID`，玩家丟棄物與 PK 掉落會繼承所在場景，撿取、掉落廣播、玩家進場、移動拒絕回補、VisibilitySystem、傳送與死亡重建都改用同 ShowID 地上物查詢，避免不同副本/場景的地上物被看到、撿走或塞回 Known。
- 修正地上物 TTL 到期移除廣播：`GroundItemSystem.Update()` 改依地上物自己的 `ShowID` 發送 `S_RemoveObject`，避免不同副本玩家收到到期物件消失封包。
- 新增地上物 ShowID 回歸測試：鎖定 `GetNearbyGroundItemsInShow`、掉落廣播、到期移除、跨 ShowID 撿取拒絕、visibility、rejectMove、傳送與死亡重建不會跨 ShowID 送出地上物。
- 收斂一般戰鬥與 PvP 的 ShowID 邊界：玩家近戰/遠攻打 NPC 時會拒絕不同 ShowID 目標，近戰、遠攻、field object、木人、武器損壞特效、PvP 攻擊、粉名、正義值與 PK 掉落廣播都改用同 ShowID viewer，避免不同副本/場景玩家看到或參與戰鬥封包。
- 新增戰鬥/PvP ShowID 回歸測試：鎖定玩家不能攻擊不同 ShowID NPC/玩家，攻擊動畫、PvP 遠近攻、粉名/正義值、PK 粉名解除與掉落物件不可送給不同 ShowID 觀眾。
- 收斂玩家攻擊技能傷害/視覺的 ShowID 邊界：`skill_damage.go` 的玩家目標、NPC 目標、起死回生術、三重矢收尾與主要攻擊技能 viewer 改用同 ShowID；玩家/NPC 目標與 NPC 範圍額外目標也拒絕不同 ShowID，避免跨副本造成傷害或看到攻擊技能封包。
- 新增玩家攻擊技能 ShowID 回歸測試：鎖定不同 ShowID 玩家/NPC 不可被攻擊技能傷害，同 ShowID 攻擊技能封包與起死回生術動作不可送給不同 ShowID，NPC 範圍攻擊不可波及不同 ShowID NPC。
- 收斂自身技能與復活技能的 ShowID 邊界：`INVISIBILITY`/`BLIND_HIDING`、`DETECTION` 類自身技能、返生術/終極返生術/自然呼喚的施法動作、玩家復活顯示與寵物復活顯示改用同 ShowID viewer；玩家、NPC、寵物復活目標也拒絕不同 ShowID，避免跨副本揭隱、送復活同意或重顯示角色/寵物。
- 修正玩家死亡當下死亡動作 ShowID 廣播邊界：`DeathSystem.KillPlayer()` 改依死亡玩家 `ShowID` 發送 ACTION_Die，避免不同副本玩家看到死亡動作。
- 修正一般聊天與大喊聊天 ShowID/範圍邊界：`C_Chat` 一般聊天改走同 ShowID 可見玩家，大喊聊天改走同 ShowID 50 格查詢，對齊 yiwei `chatType_0` / `chatType_2`。
- 修正玩家快捷動作 `C_ExtraCommand` 行為：只允許 66~69，傳送中、隱身與變形限制狀態不廣播；動作封包改用自身 + 同 ShowID 可見玩家，對齊 yiwei `sendPacketsAll(new S_DoActionGFX(...))`。
- 修正個人商店開設、關閉與不可交易取消的 ShowID 廣播邊界：`PrivateShopSystem` 改用同 ShowID viewer 發送 `S_DoActionShop` / `S_DoActionGFX(3)`，避免不同副本/場景玩家看到商店動作。
- 修正 `C_Attr` 復活同意後的 ShowID 廣播邊界：復活特效、`S_Resurrection` 與角色外觀更新改送自己與同 ShowID 可見玩家，避免不同副本/場景玩家看到復活封包。
- 修正 NPC 定時聊天的 yiwei 範圍與 ShowID 邊界：一般 NPC 聊天改用同 ShowID 8 格，大喊改用同 ShowID 50 格，避免跨場景收到 NPC 聊天或大喊距離過短。
- 修正 NPC hidden 遁地/出土的 ShowID 廣播邊界：受傷遁地與玩家接近出土時的 `S_DoActionGFX` / `S_NPCPack` 改送同 ShowID 可見玩家，避免副本外玩家看到 hidden 狀態變化。
- 修正奪命之雷與骷髏毀壞的目標特效 ShowID 邊界：`THUNDER_GRAB` 4184 與 `BONE_BREAK` 13119 特效改送目標自己與同 ShowID 可見玩家，對齊 yiwei `sendPacketsAll`。
- 新增自身/復活技能 ShowID 回歸測試：鎖定隱身 remove、無所遁形揭示、復活同意、復活施法動作、玩家 put object 與寵物 remove/effect 不可跨 ShowID。
- 收斂武器/防具附魔類技能的 ShowID 邊界：`BLESSED_ARMOR`、`CREATE_MAGICAL_WEAPON`、`BRING_STONE`、`ENCHANT_WEAPON`、`BLESS_WEAPON` 的施法動作與特效改用同 ShowID 可視玩家查詢；`BLESS_WEAPON` 直接指定玩家時也會拒絕不同 ShowID 目標，避免跨副本套武器附魔。
- 新增附魔技能 ShowID 回歸測試：鎖定不同 ShowID 玩家不可收到附魔施法封包，且不同 ShowID 目標不可被 `BLESS_WEAPON` 套用。
- 收斂玩家外觀與技能狀態廣播的 ShowID 邊界：裝備外觀、隱身斗篷、變形、解除隱身、加速/勇敢、相消隱身、魔法屏障觸發特效與一般施法動作改用同 ShowID 可視玩家查詢，避免不同副本/場景玩家收到外觀或狀態封包。
- 補強玩家揭隱共用路徑：新增 `SkillSystem.sendPlayerReappearToVisible()`，讓解除隱身、相消清 buff、GM 清狀態與 `ARM_BREAKER`/`FREEZING_BREATH` 揭隱共用同 ShowID `S_PUT_OBJECT` 廣播，避免後續揭隱路徑落回 raw AOI。
- 新增玩家外觀/狀態 ShowID 回歸測試：裝備外觀、隱身斗篷、變形、技能解除隱身、速度/勇敢、相消隱身、魔法屏障特效與施法動作都鎖定不同 ShowID 不可收到封包。
- 新增揭隱 ShowID 回歸測試：鎖定 `ARM_BREAKER` 與 `GMClearAllStatuses` 解除隱身時，不同 ShowID 玩家不可收到目標重新顯示封包。
- 收斂共用傳送與死亡重啟的 ShowID 場景重建：`teleportPlayer()` 與 `DeathSystem.ProcessRestart()` 的舊位置 remove、新位置 put、玩家自身 Known 重建 now 改用 ShowID-aware nearby 查詢，避免傳送/回村後不同副本物件被重新送入客戶端。
- 補齊傳送與死亡重啟時的隨身祭司處理：玩家傳送會同步移除/傳送/重顯示自己的 hierarch，死亡重啟也會把同 ShowID 祭司寫回 Known。
- 新增傳送/死亡重啟 ShowID 回歸測試：`TestTeleportPlayerRebuildsOnlySameShowObjectsLikeJava`、`TestDeathRestartRebuildsOnlySameShowObjectsLikeJava`。
- 收斂玩家進場、移動拒絕回補與 VisibilitySystem 的 ShowID 可視邊界：玩家、NPC、召喚物、娃娃、祭司、follower、寵物的感知與重建 Known 現在改用 ShowID-aware nearby 查詢，避免不同副本/場景的物件被重新送回客戶端或塞回 Known。
- `world.State` 補上 companion/NPC visibility 專用的 `GetNearby*InShow` 查詢，保留既有 raw 查詢給非 ShowID 場景，讓後續排查能優先改共用入口而不是每條廣播路徑各補一次特判。
- 新增 visibility / movement ShowID 回歸測試：`TestVisibilityPerceivesOnlySameShowObjectsLikeJava`、`TestMoveAndDirectionBroadcastOnlySameShowLikeJava`、`TestRejectMoveRebuildsOnlySameShowKnownObjectsLikeJava`。
- 進一步收斂 yiwei companion ShowID 邊界：`companion_ai.go` 內所有 companion 廣播不再直接使用 raw `GetNearbyPlayersAt`，召喚物過期、娃娃/祭司瞬移、寵物死亡、手動解散/收回、進化、改名、釋放回野生 NPC 等路徑改走 `companionViewersAt()`，避免不同副本玩家看到 remove、死亡動作、重生外觀或技能特效。
- 釋放召喚物、解散寵物與 follower 還原原 NPC 時，回生的 NPC 會繼承原 companion ShowID，外觀封包只送同 ShowID 可見玩家。
- 新增 companion ShowID 回歸測試：過期召喚物 remove、娃娃/祭司瞬移 remove/put、手動解散 summon/pet/doll/hierarch、寵物死亡動畫都鎖定不同 ShowID 不可收到封包。
- 對齊 yiwei mob skill type 2 施法射程：NPC `magicAttack()` 先經 `L1SkillUse.checkUseSkill()` / `makeTargetList()`，Go 現在於共用 `executeNpcSkill()` 依技能表 `skill.ranged` 擋掉射程外目標；`ranged=-1` 時改走 Java `isInScreen()` 畫面範圍判定。失敗時不消耗 MP、不造成傷害、不進冷卻、不累計 TriCount，取代原本只針對 `87 SHOCK_STUN` 的射程特判。
- 對齊 yiwei NPC target skill 目標合法性：非 87 的 type 2 目標技能現在也會在共用 `executeNpcSkill()` 入口拒絕死亡、不同 map/showId、GM 隱身與絕對屏障玩家；`checkUseSkill=false` 只讓技能失敗，不再清掉怪物仇恨目標，避免 stale target 被怪物魔法打到。
- 對齊 yiwei type 2 AoE 額外目標篩選：範圍魔法傷害迴圈改用同一套 NPC target gate，排除不同 showId、GM 隱身、死亡、絕對屏障與對 NPC 隔牆的 non-through 玩家，施法動畫也只廣播給同 showId 玩家，避免副本外或牆後玩家被波及或看到技能。
- 對齊 yiwei type 2 `target=none` 攻擊魔法：NPC 施放時依 Java 將技能目標重設為施法者自身，`ranged=0` 不再因仇恨玩家距離失敗，範圍傷害中心改為 NPC 座標，讓自中心範圍魔法可正確波及身旁玩家而不誤以仇恨目標為中心。
- 對齊 yiwei mob skill type 2 魔法傷害倍率：NPC `magicAttack()` 成功施放時會套用 mobskill `leverage` 到攻擊魔法傷害，Go 現在於單體與範圍魔法 damage commit 前按 `damage * leverage / 10` 調整，避免怪物魔法技能強度無視資料表倍率。
- 對齊 yiwei mob skill type 2 魔法施放冷卻：NPC template/runtime/轉檔與現有 YAML 補上 `atk_magic_speed`，type 2 成功施放 `target=attack` 且非 `18 TURN_UNDEAD` 時使用 `AtkMagicSpeed`，自補/輔助類使用 `SubMagicSpeed`，避免攻擊魔法錯吃普攻速度。
- 接通 yiwei mob skill type 1 前方範圍物理攻擊：`area_height > 0` 時依 Java `World.getVisibleBoxObjects()` 的旋轉 box 選取同 ShowID、前方、LOS 可見玩家逐一套物理傷害；主目標以外的前方玩家也會受傷，側邊、背後與不同 ShowID 玩家不會被納入。
- 對齊 yiwei mob skill type 1 物理技能距離欄位：`mob_skill_list.yaml` 的 `range/area_width/area_height` 現在會載入並傳到 Lua/System，type 1 物理 mob skill 依 Java `physicalAttack()` 在距離超過 `range` 時失敗，不消耗 TriCount、不進攻擊冷卻，也不隔空造成傷害。
- 接通 yiwei mob skill type 14 範圍 debuff：Lua 分流 `area_debuff` 並跳過 MP gate，NPC AI 對同 ShowID 可見玩家依 Java `areadebuff()` 套用 mobskill `skill_id`，`leverage` 作為 buff 秒數，保留 `gfx_id` 目標特效與 `act_id` 動作；71 會跳過已有 4011 藥水侵蝕術者，成功後走共用 TriCount/reuseDelay/`sub_magic_speed` 收尾。
- 接通 yiwei mob skill type 16 召喚特效物件：Lua 分流 `spawn_effect` 並跳過 MP gate，NPC AI 依 Java `SpawnEffect()` 在施法者座標生成 `230270/gfx12750` L1Effect、map 93 與空 summon 失敗不消耗次數，成功後沿用共用 TriCount/reuseDelay/`sub_magic_speed` 收尾。
- 接通 yiwei mob skill type 17 範圍詛咒麻痺：Lua 分流 `area_curse_paralyze` 並跳過 MP gate，NPC AI 對同 ShowID 可見玩家依 Java `areams()` 固定套 `33 CURSE_PARALYZE` 延遲麻痺階段、送 212 與灰色色調，成功後走共用 TriCount/reuseDelay/`sub_magic_speed` 收尾。
- 接通 yiwei mob skill type 7 範圍壞物術：Lua 分流 `area_weapon_break` 並跳過 MP gate，NPC AI 依怪物 `intel/3` 損壞同 ShowID 可見玩家的裝備武器、播放 172、送 268 與 S_ItemStatus；同時把 area/summon/poly 類 mob skill 成功後的 TriCount/reuseDelay/`sub_magic_speed` 記錄收斂成共用 helper。
- 接通 yiwei mob skill type 9/10 範圍污濁的水流與治癒侵蝕術：Lua 分流 `area_pollute_water_wave` / `area_heal_turn_to_damage` 並跳過 MP gate，NPC AI 共用 4011/4012/4013 特殊狀態 helper，分別套 4012/4013 12 秒並播放 7782/7780，成功後記 TriCount/reuseDelay 與 `sub_magic_speed`。
- 接通 yiwei mob skill type 8 範圍藥水侵蝕術：Lua 分流 `area_potion_turn_to_damage` 並跳過 MP gate，NPC AI 對同 ShowID 可見玩家套 `4011` 12 秒、播放 7781，且與 4011/4012/4013/71 互斥；成功後記 TriCount/reuseDelay 並使用 `sub_magic_speed`。
- 修正 NPC 範圍 mob skill 目標特效廣播來源：type 11 範圍沉默與 type 13 範圍風之枷鎖改依 yiwei `pc.sendPacketsAll(...)` 從被套用玩家座標廣播目標特效，避免目標旁但不在怪物旁的玩家看不到 10708 或 mobskill `gfx_id` 特效。
- 優化 NPC `spawnEffect` 地面效果生成路徑：抽出 `spawnNpcGroundEffectLikeJava()` 共用 NPC caster 的 L1Effect 生成、廣播與 Known 記錄，讓 `87 SHOCK_STUN`、mob skill type 5 與 type 15 毒霧共用同一套 ShowID 可見性邏輯，並修正不同 ShowID 玩家仍收到 `81162` 效果可見封包的問題。
- 修正 `172 STORM_WALK` 勇敢速度互斥清單：改清 `52 HOLY_WALK` 而非 `42 PHYSICAL_ENCHANT_STR`，並補清 `155 FIRE_BLESS`，避免暴風疾走誤移除力量加成或漏清烈炎氣息。
- 對齊 yiwei `mobskill.sql` 欄位順序重新產生 `mob_skill_list.yaml`，修正怪物技能資料錯位導致 `skill_id`、`leverage` 等欄位錯誤，怪物技能 fallback 成普攻的問題。
- 對齊 Java `L1NpcInstance.noTarget()` 的怪物無目標隨機移動：0-7 才移動，8-39 只等待，降低怪物無目標時過度移動頻率。
- 補上 NPC `HiddenStatus` 遁地基礎狀態機：對齊 yiwei `initHide()` 與 `hide()` 的史巴托系 NPC 出生遁地、受傷低於 1/3 HP 機率再遁地、玩家 2 格內接近時出土並鎖定目標。
- 擴充高崙 hidden 行為：`45045/45126/45134/45281` 石頭高崙 1/3 機率以 status 4 遁地，`46125-46128` 冰藏 status 3/status 4；玩家近戰、弓攻與技能目標會依 Java 拒絕 hidden sink/fly 目標，194/213 等揭露技能可讓 sink hidden 顯形，避免 hidden 高崙仍被打到。
- 修正怪物隔牆打人：NPC 近戰與 type 1 物理 mob skill 攻擊前套用 Java `isAttackPosition()` / `glanceCheck()` 對應的 LOS 檢查。
- 對齊 yiwei `L1NpcInstance.attack()` 的 mob skill 入口 LOS 門檻：怪物目前仇恨目標被牆擋住時，不把 mob skill 清單交給 Lua，也不執行自放/召喚/範圍輔助技能命令，避免隔牆對自己放輔助技能。
- 對齊 yiwei NPC 目標選取條件：怪物自動掃描目標時依 ShowID、隱身、GM、死亡/HP、移動可達性與 LOS 過濾，既有仇恨目標跨 ShowID 或失效時會清除並回退到下一個有效仇恨目標。
- 修正 NPC 生成選點的 `avoid_pc` / `respawn_screen` 順序：對齊 yiwei `L1Spawn.doSpawn()` 先避開玩家、再判斷是否允許出生在玩家畫面中，避免 `respawn_screen=true` 反而忽略 `avoid_pc`。
- 對齊 yiwei mob group 連動：群體怪物受攻擊時，空仇恨隊員會以 0 hate 鎖定攻擊者；隊員生成時繼承 leader ShowID，並依 `initHideForMinion()` 繼承 sink/fly/ice hidden 狀態。
- 對齊 yiwei `L1NpcInstance.serchLink()` 的 `family/agrofamily` 同族支援 hate 行為：NPC template/runtime 增加 `family`、`agro_family`，玩家攻擊或玩家技能造成 NPC hate 後，附近同 ShowID、未已有 hate、符合同族或全體支援規則的 NPC 以 0 hate 連動；同步補 SQL/YAML 轉檔欄位、合併 yiwei NPC family 資料與回歸測試。
- 對齊 yiwei 人物屬性戰鬥影響：Lua STR/DEX/INT 查表改用 `L1AttackList` 0..127 規則，近戰命中只吃 STR、遠攻傷害只吃 DEX/弓傷/箭傷；攻擊魔法傷害係數吃 INT + 額外 SP，魔法爆擊吃 INT_CRIT 與額外魔爆；心靈破壞與舊版武器魔法特效改用 Java `getSp()` / INT 魔法命中規則。
- 修正人物屬性魔攻對齊：攻擊魔法不再把 true SP 額外擲成傷害骰，改回 yiwei `L1MagicPc.calcMagicDiceDamage()` 只擲 `damageDiceCount`；新增近戰 STR、遠攻 DEX、魔攻 INT/額外 SP、Base STR/DEX/INT 純能力加成與 true SP 回歸測試。
- 補齊 `87 SHOCK_STUN` 玩家施放的 Java `glanceCheck` 門檻：射程內但隔牆的玩家/NPC 目標會在 MP/資源消耗前被拒絕，不套用暈眩、不送 4434、不建立 81162 效果 NPC。
- 補齊 `87 SHOCK_STUN` 的 Java `showId` 目標隔離：玩家/NPC 單體衝暈與 mob skill type 5 範圍衝暈都只作用於同 ShowID 可見目標，避免跨副本/場景消耗資源、套用 87 或廣播動作。
- 補齊 `87 SHOCK_STUN` 的 hidden NPC 目標阻擋：玩家衝暈遁地/飛天 NPC 時對齊 Java `isTarget()` 在消耗前拒絕，不觸發 onAction、不清睡眠/ERASE_MAGIC、不套 87。
- 補齊 `87 SHOCK_STUN` 的 NPC 類型與 Door 目標判定：對齊 Java 在消耗前拒絕 `L1Effect`、`L1Illusory`、`L1Doll`、`L1Hierarch`、不可破壞 `L1Door`；可破壞 `L1Door` 保留 onAction 但不套 87/4434/81162。
- 補齊 yiwei 物理爆擊公式：Lua 新增 `STR_CRIT` / `DEX_CRIT` 0..127 查表，普通近戰依 STR 爆擊將武器傷害提升為最大值兩倍，普通遠攻依 DEX 爆擊將武器傷害提升為最大值；新增 STR/DEX 爆擊回歸測試。封包 attack type 與爆擊特效 NPC 仍屬廣域視覺缺口。
- 修正攻擊魔法 MR 減傷公式：對齊 yiwei `L1MagicPc.calcMrDefense()`，以施法者 `INT_MAGIC_HIT`、Base INT 純命中與 `OriginalMagicHit` 抵銷目標 MR 後再計算減傷，避免高 INT / 魔法命中裝備的攻擊魔法仍被完整 MR 壓低。
- 補齊遠距離攻擊 ER 迴避判定：對齊 yiwei `L1AttackMode.calcErEvasion()`，PC 弓攻與 NPC `ranged >= 10` 且距離 2 格以上的遠攻，在基礎命中後再用目標 ER/Dodge 執行 1..3000 二段迴避，讓堅固防護、迴避提升、水之防護等 ER 來源實際影響遠距離命中。
- 補齊玩家有效 ER `getEr()` 動態公式：遠攻 ER 迴避與 `UPDATE_ER` 封包改用職業等級、DEX 與 Dodge/ER 累積值重算後的有效 ER，`STRIKER_GALE` 狀態下回傳 0；`DRESS_EVASION` 封包值修正為有效 ER 總值而非單一技能增量。
- 對齊 yiwei mob skill `TriCount` 使用次數上限：`trigger_count` 現在會載入到 Go，NPC AI 依每隻 NPC、每個 `act_no` 記錄成功施放次數，達到上限後不再交給 Lua 決策，避免有限次技能被無限重複施放。
- 對齊 yiwei mob skill `change_target=3` 隨機換目標：NPC 施放技能前會依可見、同 ShowID、仇恨列表、TriRange 與 LOS 篩選候選玩家，沒有候選才退回原目標，避免隨機目標技能固定打當前目標。
- 對齊 yiwei mob skill `TriCompanionHp` 同族支援技能：`trigger_companion_hp` 現在會載入並在 NPC AI 決策前挑選同 ShowID、同 family、低於門檻的同族 NPC 作為技能目標，讓幫補/幫加速類怪物技能能作用在同族 NPC，而不是誤打玩家。
- 對齊 yiwei mob skill `reuseDelay` 共通冷卻：`mobskill.sql` 第 21 欄會轉入 `mob_skill_list.yaml`，NPC 成功施放後依每隻怪、每個 `act_no` 設定冷卻，冷卻內不再交給 Lua 決策，避免怪物技能連發過密。
- 對齊 yiwei mob skill type 級冷卻：type 3 召喚與 type 4 群體變身成功後會同步鎖定同類型技能冷卻，避免同一隻怪在 act_no 冷卻期間改放另一個召喚技能。
- 對齊 yiwei mob skill type 3 召喚成功語義：map 93 與無召喚 ID 時不記 TriCount/reuseDelay，也不進行召喚；Go 缺 NPC 模板時因無法實際生成同樣不記成功；召喚成功後改用 `sub_magic_speed` 冷卻。
- 接通 yiwei mob skill type 4 群體變形：載入 `poly_id`，Lua 回傳 `poly` command，NPC AI 對可見玩家依 LOS/死亡/GM 隱身篩選後執行 1800 秒 NPC cause 變形，成功後才記次數/冷卻並使用 `sub_magic_speed`。
- 對齊 yiwei NPC SILENCE 施法消費語義：NPC 持有 `64 SILENCE` 時，單體/自我/同族輔助 mobskill 會移除沉默並讓該次施法失敗，不回血、不扣 MP、不記 TriCount/reuseDelay，下一次才可正常施法。
- 接通 yiwei mob skill type 11 範圍沉默：Lua 分流 `area_silence`，NPC AI 對同 ShowID 可見玩家套 `64 SILENCE` 16 秒並播放 10708，已有 64/161 者不重套；成功後記 TriCount/reuseDelay 並使用 `sub_magic_speed`。
- 接通 yiwei mob skill type 6 範圍魔法相消：Lua 分流 `area_cancellation`，NPC AI 對同 ShowID 可見玩家執行既有 `CancelAllBuffs`，可相消 buff 會移除、不可相消 buff 保留；成功後記 TriCount/reuseDelay 並使用 `sub_magic_speed`。
- 接通 yiwei mob skill type 13 範圍風之枷鎖：Lua 分流 `area_wind_shackle`，NPC AI 對同 ShowID 可見且未持有 167 的玩家套 `167 WIND_SHACKLE` 16 秒、播放 mobskill `gfx_id` 並送 WindShackle 封包；成功後記 TriCount/reuseDelay 並使用 `sub_magic_speed`。
- 接通 yiwei mob skill type 12 範圍藥水霜化：Lua 分流 `area_decay_potion`，NPC AI 硬套 Java 指定的 `71 DECAY_POTION`，已有 71 者不刷新；成功後記 TriCount/reuseDelay 並使用 `sub_magic_speed`。
- 對齊 yiwei NPC AI mob skill type 15 集體毒霧：Lua 回傳 `area_poison`、跳過 MP gate，NPC AI 依 `summon_id/leverage/act_id` 生成 `86125` 毒霧效果，GroundEffectSystem 每秒套 100 傷害毒，並補上 `86125` L1Effect YAML 模板與回歸測試。
- 補齊 yiwei mobskill 實際引用的 488 筆技能模板至 `skill_list.yaml`（保留既有 1~253 手工對齊資料，不重產整表），避免 type 2 怪物技能因 `skill == nil` 退回近戰；新增資料測試鎖定 mobskill 引用技能必須可解析，僅允許 yiwei `skills.sql` 本身缺模板的 4017/41583/41606/41607。
- 修正 yiwei type 2 `target=none` 攻擊魔法的 LOS 與動畫目標：NPC 施放時依 Java 將技能目標視為施法者自身，仇恨玩家即使隔牆也不會阻止自中心範圍技能施放，範圍傷害仍逐目標檢查牆與 showId。
- 修正 PvP 物理公式 context：近戰與遠攻都會把目標 `ClassType` 傳入 Lua，讓 yiwei 的玩家職業 AC 防禦減傷不再錯套成 class 0，並補上 `LikeYiwei` 回歸測試。
- 對齊 yiwei 補血公式：治療量改以 `damage_value + getMagicBonus(INT)` 作為擲骰次數，忽略 `damage_dice_count` 與額外 SP，並套用施法者 lawful 與 leverage；所有玩家治療路徑改傳施法者 INT/lawful，水之元氣與污濁之水測試改以 deterministic 治療量鎖定倍率。
- 對齊 yiwei NPC mobskill 自補與同族補血公式：runtime `NpcInfo` 現在保留模板 `INT`，怪物治療改用 `L1MagicNpc.calcHealing()` 的 `damage_value + npc.getMagicBonus()` 擲骰次數，並套用 NPC lawful 與 mobskill leverage，避免有智力的怪物補血仍只補固定一骰。
- 修正 NPC mobskill 自補與同族補血的 ShowID 廣播邊界：對齊 yiwei `L1Character.broadcastPacketAll()`，自補/幫補特效與 HP meter 只送給同 ShowID 可見玩家，避免副本外玩家看到怪物補血效果。
- 修正 NPC mobskill 召喚的 ShowID 邊界：召喚怪繼承 caster ShowID，召喚特效與新 NPC 顯示封包只送給同 ShowID 可見玩家，對齊 yiwei `L1MobSkillUseSpawn.mobspawn()` 與 `broadcastPacketAll()`。
- 修正 NPC 一般移動與遊走的 ShowID 廣播邊界：對齊 yiwei `NpcMove.setDirectionMove()` 走 `broadcastPacketAll()` 的行為，NPC 移動封包只送同 ShowID 可見玩家，避免副本外玩家看到怪物位移。
- 修正 NPC 回家傳送的 ShowID 廣播邊界：一般 NPC 與守衛回出生點時，舊位置 remove 與新位置 NPC 顯示封包改走 `nearbyNpcViewersAt()`，只送同 ShowID 可見玩家，避免副本外玩家看到怪物回家消失或重現。
- 收斂 NPC AI 的玩家可視查詢：守衛搜尋通緝犯、mobskill type 1 物理技能動作、NPC debuff/poison 色調與毒傷 HP meter 全部改走 ShowID-bound viewer helper，對齊 yiwei `getVisiblePlayer()` / `broadcastPacketAll()` 的副本隔離語義。
- 修正毒色調廣播邊界：玩家中毒 `S_Poison` 改送自己與同 ShowID 可見玩家；NPC 傷害毒初始與清除色調改用同 ShowID 8 格 viewer，對齊 yiwei PC `sendPacketsAll` 與 NPC `setPoisonEffect()` / `broadcastPacketX8`。
- 新增毒色調 ShowID/X8 回歸測試：鎖定玩家中毒不可跨 ShowID，NPC 傷害毒初始、tick 清除與 debuff 到期清除不可送給不同 ShowID 或 8 格外玩家。
- 修正 NPC respawn/生成 ShowID 邊界：NPC 屍體移除、重生顯示與 mob group 隊員生成顯示封包改用同 ShowID viewer，對齊 yiwei NPC `onPerceive()` / `broadcastPacketAll()` 的副本隔離，避免副本外玩家看到怪物消失、重生或隊員生成。
- 對齊 yiwei companion ShowID 行為：召喚物、寵物、魔法娃娃、祭司、跟隨物新增 runtime ShowID，生成時繼承主人 ShowID，外觀生成與共用移動廣播改走 ShowID-aware viewer helper，避免不同副本玩家看到 companion 生成或移動封包。
- 新增 `TestCompanionSummonMoveBroadcastsOnlySameShowLikeJava`，鎖定召喚物移動封包只能發給同 ShowID 玩家。
- 新增 `handler.BroadcastToVisiblePlayers` 共用 ShowID-aware 廣播 helper，釣魚動作與陷阱地點特效改走同一條路，避免 raw AOI 把不同副本/ShowID 玩家一起廣播。
- 2026-05-27：收斂 NPC 服務傳送出發特效廣播，`NpcTeleportWithCost` 改用預組 `S_SkillSound(169)` 並透過 `BroadcastToVisiblePlayers` 傳給同 ShowID 可見玩家，避免不同副本/ShowID 玩家收到離場特效。
- 2026-05-27：收斂寵物補血/加速道具效果廣播，`PetSystem.GiveToPet` 的補血特效、`S_SkillHaste` 與 191 音效改走 `BroadcastToVisiblePlayers`，避免不同副本/ShowID 玩家看到寵物吃藥效果。
- 2026-05-27：收斂傳送頁出發特效廣播，`executeTeleportPage` 的 169 離場特效改用預組封包與 `BroadcastToVisiblePlayers`，避免不同副本/ShowID 玩家看到 NPC 傳送頁離場效果。
- 2026-05-27：收斂玩家對 NPC debuff 的目標與廣播 ShowID 邊界，`executeNpcDebuffSkill` 現在拒絕不同 map/ShowID NPC 目標，並以 `GetNearbyPlayersInShow` 發送 NPC debuff 動作與特效，避免跨副本加仇恨或看到弱點/狀態特效。
- 2026-05-27：對齊 yiwei mobskill type 1 物理技能 `act_id` 覆寫，NPC 物理 mobskill 現在會把 `mobskill.act_id` 寫入攻擊封包 action byte，讓史巴托/高崙等特殊攻擊動作不再退回普通 action 1。
- 2026-05-27：對齊 yiwei NPC AI `_sleepTime` 行動節奏，NPC 攻擊與 mobskill 成功後的攻擊/魔法冷卻現在同步鎖定 `MoveTimer`，避免怪物在技能或攻擊 sleep 期間仍繼續追擊移動。
- 2026-05-27：對齊 yiwei `L1MobSkillUse.skillUse()` 的候選技能隨機選招，Lua NPC AI 現在會先收集所有可用 mobskill command 再隨機挑選，避免永遠優先施放 YAML 第一筆技能。
- 2026-05-27：對齊 yiwei `L1MobSkillUse.isSkillUseble()` 的候選 gate，mobskill 需至少符合 HP、同族 HP、距離或次數其中一個觸發條件才會進入候選，避免只有 `trigger_random` 的技能被 Go 當成常駐技能施放。
- 2026-05-27：對齊 yiwei mobskill `trigger_random` 判定時機，Lua NPC AI 現在會先隨機選中候選技能，再對該技能做發動機率判定；若機率失敗則退回一般 AI，不再 fallback 到其他候選技能。
- 2026-05-27：對齊 yiwei `L1MobSkill.isTriggerDistance()` 的 `trigger_range` 正負距離語義，負值代表距離小於等於絕對值、正值代表距離大於等於該值；NPC 自我施法也不再跳過 TriRange 判定。
- 2026-05-27：對齊 yiwei `trigger_companion_hp` 混合條件語義，沒有同族 NPC 時不再提前排除技能，會繼續檢查 `trigger_range` / `trigger_count`；有同族但血量未達門檻時仍跳過該技能。
- 2026-05-27：補齊 yiwei `L1SkillUse.sendGrfx()` 補助/詛咒魔法後置狀態刷新；self buff 與一般 PC 目標 buff 成功施放後會送 `S_SPMR`、`S_OwnCharStatus`、`UPDATE_ER` 給目標 PC。
