## 地圖與生成

- 以 yiwei `mapids.sql` 重產 `map_list.yaml`，並同步本機 `server/map` 讓怪物生成與碰撞判斷使用對齊的地圖資料。
- 調整 `sqlconv spawn`：怪物生成改用 yiwei `spawnlist.sql`，普通 NPC 生成則以既有 `spawn_list.yaml` 為基底完整保留，不再用 yiwei `spawnlist_npc.sql` 覆蓋商人、傳送師與村莊 NPC。
- 重產 `spawn_list.yaml` 為「yiwei 怪物 + 原本普通 NPC」：普通 NPC 保留 2372 筆、總 spawn 7808 筆，並過濾目前 `npc_list.yaml` 不存在樣板的怪物 spawn，避免啟動時輸出找不到 NPC 樣板警告。
- 修正 NPC 生成與重生選點，優先尋找可通行且未佔用座標，避免怪物生成到牆內或大量重疊；若地圖 tile 檢查全部失敗，保留 yiwei 初始化生成的 fallback 行為，避免整筆 spawn 消失。

## 戰鬥與視線

- 補齊玩家近戰、遠攻、PvP 物理攻擊與範圍技能的 LOS 判定，避免隔牆攻擊與隔牆範圍傷害；NPC 與召喚物移動也改為缺少通行資料時不硬走穿牆。

## 技能
- 修正擺攤角色施放 `SHOCK_STUN` 的入口邊界，對齊 Java `C_UseSkill.start()` 在 `pc.isPrivateShop()` 時直接返回；擺攤中不再排入 `SkillQueue`。
- 修正玩家施放 `SHOCK_STUN` 的射程與消耗前邊界，對齊 Java `L1SkillUse` 以技能資料 `ranged=1` 在 `useConsume()` 前判定；玩家與 NPC 目標超過 1 格時不消耗 MP/資源、不進入 onAction、不套用 87，也不清除目標既有睡眠或魔法消除狀態。
- 修正玩家施放 `SHOCK_STUN` 對 GM 隱身玩家的目標邊界，對齊 Java `L1SkillUse.checkTarget()` 會排除 `pc.isGhost()` / `pc.isGmInvis()`；被排除時不套用 87、不觸發 onAction，也不清除目標既有睡眠或魔法消除狀態。
- 修正 `SHOCK_STUN` 的 `ERASE_MAGIC(153)` 移除語義，對齊 Java `L1SkillUse.runSkill()` 概率技能結果處理：玩家施放到玩家與 NPC 目標、NPC 單體施放到玩家目標時，會先移除目標既有魔法消除狀態。
- 修正玩家施放 `SHOCK_STUN` 到玩家與 NPC 目標時的睡眠解除順序，對齊 Java `L1SkillUse.runSkill()`：87 判定結果處理前會先移除 `FOG_OF_SLEEPING`、`212`、`103` 睡眠效果，玩家目標同步送出 `S_Paralysis` 睡眠解除。
- 修正玩家施放 `SHOCK_STUN` 的安全區邊界，對齊 Java `L1MagicPc.checkZone()` / `checkZoneDE()` 的 NZONE 行為；施法者或目標在安全區時送出「在安全區域無法使用此技能。」且不套用 87、不送目標 4434。
- 修正 NPC 單體施放 `SHOCK_STUN` 的 Java 成功率上限，對齊 `L1MagicNpc.calcProbabilityMagic()` 封頂 90%；玩家施放路徑仍維持 `L1MagicPc` 的 100% 上限。
- 修正 NPC 單體施放 `SHOCK_STUN` 對已被 `ICE_LANCE(50)` / `EARTH_BIND(157)` 控制玩家目標的 Java 邊界；NPC 施放路徑會保留施法者 action，但不套用 87、不送目標 4434。
- 修正 `SHOCK_STUN` 對已被 `ICE_LANCE(50)` / `EARTH_BIND(157)` 控制目標的 Java 邊界；玩家與 NPC 目標路徑會保留 onAction 副作用，但不套用 87、不送目標 4434。
- 修正玩家施放 `SHOCK_STUN` 到玩家目標時的 4434 特效廣播來源，對齊 Java `pc.sendPacketsAll(new S_SkillSound(pc.getId(), 4434))`；玩家目標成功與既有 87 路徑會以目標座標廣播固定 4434。
- 修正玩家施放 `SHOCK_STUN` 非空目標時的施法者動作封包，對齊 Java `L1SkillUse.sendGrfx()` 的 87 特例只送目標固定 4434 後返回；玩家目標成功與既有 87 目標路徑不再廣播施法者 `S_DoActionGFX`。
- 修正玩家施放 `SHOCK_STUN` miss 時的施法者動作封包，對齊 Java `L1SkillUse.sendGrfx()` 在 `_targetList.size()==0` 直接返回；玩家目標與 NPC 目標 miss 都不再廣播施法者 `S_DoActionGFX`。
- 補齊 `SHOCK_STUN` mob skill type 5 範圍衝暈冷卻所需的 NPC `sub_magic_speed` 資料鏈，NPC YAML schema、sqlconv/台版轉檔、runtime NPC 建立流程都會保留 `SubMagicSpeed`，並從 yiwei `npc.sql` 合併 1457 筆非零資料。
- 修正 `SHOCK_STUN` mob skill type 5 範圍衝暈成功後的 NPC 冷卻來源，對齊 Java `L1MobSkillUse.areashock_stun()` 使用 `getSubMagicSpeed()`；Go 現在優先使用 `SubMagicSpeed`，沒有資料時才回退攻擊速度。
- 修正 `SHOCK_STUN` mob skill type 5 範圍衝暈在 map 93 的 Java 邊界，對齊 `L1MobSkillUse.areashock_stun()` 會 `return false`；Go 現在不套用效果、不廣播動作，也不設定 NPC 技能冷卻。
- 修正 NPC 單體施放 `SHOCK_STUN` 對 GM 隱身玩家的目標邊界，對齊 Java `L1SkillUse.checkTarget()` 會排除 `pc.isGhost()` / `pc.isGmInvis()`；被排除時不解除睡眠、不套用 87，也不送施法動作或目標特效。
- 修正 NPC 施放 `SHOCK_STUN` 的睡眠解除順序，對齊 Java `L1SkillUse.runSkill()` 會在概率技能命中判定前移除 `FOG_OF_SLEEPING`、`212`、`103`；即使命中失敗也會先送 `S_Paralysis` 睡眠解除。
- 修正 NPC 施放 `SHOCK_STUN` 的 mob_skill `leverage` 命中倍率，對齊 Java `L1MagicNpc` 會將 `70/50/30` 基準乘上 `leverage / 10` 後再扣暈眩抗性；同時避免 87 因 `leverage > 0` 被誤分流成 type 1 物理技能。
- 修正 NPC 施放 `SHOCK_STUN` 到已有 87 目標時的命中判定順序，對齊 Java 會先做 `L1MagicNpc.calcProbabilityMagic()`，失敗時只送施法者 `S_DoActionGFX`，不送目標 `S_SkillSound(4434)`。
- 修正 NPC 施放 `SHOCK_STUN` 的廣播範圍，對齊 Java `L1SkillUse` NPC buff 路徑從施法 NPC `_user.broadcastPacketAll(...)` 送出施法動作與目標特效，不再用目標座標決定收包玩家。
- 修正 NPC 施放 `SHOCK_STUN` 的目標特效來源，對齊 Java `L1SkillUse.checkUseSkill(... gfxid ...)` 會優先使用 mob_skill `gfx_id`，只有 0 才回退技能表 `cast_gfx`。
- 修正 NPC 施放 `SHOCK_STUN` 的施法動作來源，對齊 Java `L1SkillUse.checkUseSkill(... actid ...)` 會優先使用 mob_skill `act_id`，只有 0 才回退技能表 `action_id`。
- 修正 NPC 施放 `SHOCK_STUN` 被暈眩抗性抵抗時的 Java 封包語義，失敗時只廣播施法者 `S_DoActionGFX`，不套用 87、不生成 81162 效果 NPC，也不送目標 4434。
- 修正 NPC 施放 `SHOCK_STUN` 到已有 87 狀態玩家時的 Java 封包語義，不刷新暈眩、不再次生成 81162 效果 NPC，但仍會廣播施法者 `S_DoActionGFX` 與目標固定 4434。
- 補齊 NPC 施放 `SHOCK_STUN` 成功時的 Java 施法動作封包，對齊 `L1SkillUse` 的 NPC buff 路徑會廣播施法者 `S_DoActionGFX`，並保留目標固定 4434 與 81162 效果 NPC。
- 補齊 `SHOCK_STUN` 相關聯盟呼喚接受的 `C_ATTR mode=4976` 路由，對齊 Java `C_Attr case 4976` / `callClan1()`；同聯盟成員接受穿雲箭呼喚時會傳送到呼喚者，且 `Paralyzed/Sleeped` 狀態下仍會被 SHOCK_STUN 阻擋而不傳送。
- 補齊 `SHOCK_STUN` 相關呼喚盟友接受的 `C_ATTR mode=729` 路由，對齊 Java `C_Attr case 729`；實際走 C_ATTR 回應時會委派同一段呼喚盟友處理，並保留 `Paralyzed/Sleeped` 狀態下不傳送的 SHOCK_STUN 阻擋。
- 修正玩家施放 `SHOCK_STUN` 的純 INT 成功率加成，對齊 Java `L1MagicPc` 使用 `getBaseInt()` 而非目前 INT；Go 會從目前 INT 扣除裝備與 buff 的 INT 加成後再計算 25~44 / 45+ 純 INT 命中補正，避免裝備或 buff INT 被誤算成純能力。
- 修正玩家施放 `SHOCK_STUN` 的成功率 INT 魔法命中補正，對齊 Java `L1MagicPc` 於 87 成功率納入 `L1AttackList.INTH.get(pc.getInt())`；玩家對玩家與玩家對 NPC 的 87 成功率都會依 INT 23~127 的 Java 表額外加成。
- 修正玩家施放 `SHOCK_STUN` 的成功率魔法命中加成，對齊 Java `L1MagicPc` 於 87 成功率納入 `getOriginalMagicHit()`；`PlayerInfo` 新增 `OriginalMagicHit`，玩家對玩家與玩家對 NPC 的 87 成功率都會加上此值。
- 修正玩家施放 `SHOCK_STUN` 的成功率等級比較，對齊 Java `L1MagicPc` 使用 `Level + StunLevel` 判定高於、等於或低於目標等級；`PlayerInfo` 新增 `StunLevel`，玩家對玩家與玩家對 NPC 的 87 成功率都會納入此值。
- 修正 NPC 施放 `SHOCK_STUN` 到玩家時缺少 Java 成功率判定的問題，現在依 `L1MagicNpc` 的等級差 `70/50/30` 基準並扣目標暈眩抗性；高暈抗目標抵抗時不套用 87，也不生成 81162 效果 NPC。
- 修正 NPC 目標被 `CANCELLATION` 相消時誤清除 `SHOCK_STUN` 的問題，對齊 Java `L1SkillMode.isNotCancelable()` 將 87 視為不可解除；Go 現在只移除可相消的 NPC debuff，保留 `ActiveDebuffs[87]`，並維持 Java NPC 狀態清理會重設 `Paralyzed=false` 的行為。
- 修正 `SHOCK_STUN` 與 `ABSOLUTE_BARRIER` 同時存在時的 `C_USE_ITEM` 處理順序，對齊 Java `C_ItemUSe` 先解除絕對屏障再以 `isParalyzedX()` 阻擋道具分派；玩家仍會收到 TeleportUnlock，且不會裝備或使用物品。
- 補齊 `SHOCK_STUN` 暈眩中移動封包入口邊界驗證，對齊 Java `C_MoveChar` 以 `isParalyzedX()` 擋下移動；Go `HandleMove` 已在麻痺或睡眠控制狀態直接返回，本次補上回歸測試確認座標與 `LastMoveTime` 不變。
- 補齊 `SHOCK_STUN` 暈眩中呼喚盟友接受流程阻擋，對齊 Java `C_Attr.callClan*` 在傳送前執行 `isParalyzedX()`；玩家處於麻痺或睡眠控制狀態時接受 729 呼喚盟友會清除 pending 並直接返回，不再傳送到呼喚者位置。
- 補齊 `SHOCK_STUN` 暈眩中技能封包入口阻擋，對齊 Java `C_UseSkill` 在讀取目標欄位與進入技能流程前執行 `isParalyzedX()`；玩家處於麻痺或睡眠控制狀態時直接回覆 `S_ServerMessage(285)`，不再排入 SkillQueue。
- 補齊 `SHOCK_STUN` 暈眩中攻擊封包入口阻擋，對齊 Java `C_Attack` / `C_AttackBow` 在讀取目標與進入攻擊流程前執行 `isParalyzedX()`；玩家處於麻痺或睡眠控制狀態時不再排入 CombatQueue。
- 補齊 `SHOCK_STUN` 暈眩中 `C_USE_ITEM` 共用入口使用阻擋，對齊 Java `C_ItemUSe` 在任何物品類型分派前執行 `isParalyzedX()`；玩家處於麻痺或睡眠控制狀態時使用裝備、卷軸、消耗品或其他物品會直接返回並送 TeleportUnlock。
- 補齊 `SHOCK_STUN` 暈眩中一般傳送卷軸使用阻擋，對齊 Java `C_ItemUSe` 的 `isParalyzedX()` 檢查；玩家處於麻痺或睡眠控制狀態時使用傳送卷軸會直接返回、送 TeleportUnlock，不消耗卷軸也不移動角色。
- 補齊 `SHOCK_STUN` 暈眩中消耗品使用阻擋，對齊 Java `C_ItemUSe` 的 `isParalyzedX()` 檢查；玩家處於麻痺或睡眠控制狀態時使用藥水會直接返回、送 TeleportUnlock，不消耗物品也不套用恢復效果。
- 修正 `SHOCK_STUN` 目標已有 87 狀態時的 Java 封包語義，玩家與 NPC 目標都不刷新暈眩、不重送 GM 秒數、不再次生成 81162 效果 NPC，但仍會依 `L1SkillUse` 對非空目標清單固定送目標 `S_SkillSound(4434)`。
- 補齊 `SHOCK_STUN` 的 mob skill type 5 範圍衝暈路徑，Lua AI 會將 `type=5` 分流為 `area_shock_stun`，並由 System 對可見玩家套用 Java 2~5 秒暈眩、略過 GM 隱身與已有 87 的目標、生成目標座標 81162 效果 NPC，最後廣播施法者 `S_DoActionGFX`。
- 補齊 NPC 施放 `SHOCK_STUN` 到玩家時的 Java `SHOCK_STUN.start(L1NpcInstance, ...)` 語義，改用 1~5 秒暈眩、生成目標座標 `81162` 效果 NPC，並固定送目標 4434。
- 修正 `SHOCK_STUN` NPC 目標成功特效，對齊 Java 固定送目標 `S_SkillSound(4434)`，不再依 `CastGfx` 是否為 0 決定。
- 修正 `SHOCK_STUN` 玩家目標成功特效，對齊 Java 固定送目標 `S_SkillSound(4434)`，不再依 `CastGfx` 是否為 0 決定。
- 修正 `SHOCK_STUN` NPC 目標 miss 的 Java 封包語義，只送施法動作，不再送 `S_ServerMessage(280)`。
- 修正 `SHOCK_STUN` 玩家目標 miss 的 Java 封包語義，只送施法動作，不再送 `S_ServerMessage(280)`，也不送目標 4434。
- 修正 `SHOCK_STUN` 玩家施放到 NPC 時的 Java 類別邊界，`L1Guard` 目標仍套 87 debuff 與 81162 效果，但不設為 AI 麻痺狀態。
- 修正 `SHOCK_STUN` 對已有 87 debuff 的 NPC 目標重複施放邊界，對齊 Java 不刷新時間、不重送 GM 秒數、不再次生成 81162 效果。
- 補齊 `SHOCK_STUN` 成功暈眩 NPC 時的 Java GM 秒數回饋，玩家施放到 NPC 成功時 GM 施法者也會收到 normal chat 格式的「此次衝暈秒數」訊息。
- 補齊 `SHOCK_STUN` 成功暈眩玩家時的 Java GM 秒數回饋，GM 施法者會收到 normal chat 格式的「此次衝暈秒數」訊息。
- 補齊 `SHOCK_STUN` 的 Java INT MP 消耗減免，INT > 12 時依 Java `L1SkillUse` 扣減 `INT-12` MP，讓低於原始 15 MP 但符合減免後消耗的施放可正常執行。
- 修正 `SHOCK_STUN` 成功率的 MR 設定語義，對齊 yiwei `IMPACT_HALO_MR=0` 預設不扣 MR，避免高 MR 目標被 Go 額外降低成功率。
- 修正 `SHOCK_STUN` 未裝備雙手劍時的回饋封包，對齊 Java `S_SystemMessage("請使用雙手劍")`，不再送出帶色碼與句點的 global chat。
- 修正 `SHOCK_STUN` 的隱身施法限制，對齊 Java `_cast_with_invis` 未列入 87 的行為，隱身施放時回覆 1003 並保留隱身狀態。
- 補齊 `SHOCK_STUN` 施放時的 Java `_target.onAction(_player)` 副作用，玩家與 NPC 目標路徑都會委派既有 CombatQueue 排入近戰攻擊請求。
- 修正 `SHOCK_STUN` 玩家目標 miss 時的封包語義，對齊 Java `_targetList.size()==0` 不送目標 `S_SkillSound(4434)`。
- 補齊 `SHOCK_STUN` 玩家目標成功時的 Java `L1PinkName.onAction` 副作用，改由 SkillSystem 委派 PvPSystem 執行既有粉紅名判定。
- 補齊 `SHOCK_STUN` 成功時的 Java `spawnEffect(81162)` 目標地面效果，玩家與 NPC 目標暈眩成功時會建立對應效果 NPC 並使用實際 1~5 秒暈眩時間。
- 修正 `SHOCK_STUN` 的 NPC 目標成功率，玩家施放到 NPC 時對齊 Java `IMPACT_HALO_1/2/3=40/30/10` 基準並扣 NPC MR，不再套用一般 NPC debuff 公式。
- 修正 `SHOCK_STUN` 的玩家目標成功率，對齊 Java `IMPACT_HALO_1/2/3=40/30/10` 基準並扣目標 MR 與暈眩抗性，不再套用一般 debuff 公式。
- 修正 `SHOCK_STUN` 的 Java 暈眩秒數範圍，對齊 `SHOCK_STUN_TIMER=1~5`，避免 Go 產生 6 秒暈眩。
- 補齊 `ADVANCE_SPIRIT` 的 Java MaxHP/MaxMP 上限加成語義，改以可回推的基準 MaxHP/MaxMP 五分之一計算，排除裝備、既有 buff 與魔法娃娃加成後再套用。
- 補齊 `DISINTEGRATE` 的 Java 魔法爆擊特例，究極光裂術即使是 10 級魔法仍可進入 1.5 倍魔法爆擊判定。
- 補齊 `IMMUNE_TO_HARM` 的 Java `S_PacketBox.ICON_I2H(40)` 圖示封包，新增一般 `gfx` buff icon mapping 以送出聖結界持續時間。
- 補齊 `SILENCE` 的 Java 施法白名單，沉默狀態仍允許 `87/88/89/90/91/187`，且施放白名單技能不會解除沉默。
- 補齊 `LIFE_STREAM` 的 Java 地面效果技能 ID 語義，生命之泉生成 NPC 81169 效果時以 `SkillID=0` 保存，持續時間與 HPR bonus 仍使用原技能資料。
- 補齊 `FIRE_WALL` 的 Java 逐格路徑推進語義，火牢每生成一格後會從上一個效果重新朝目標座標計算方向。
- 修正 `COUNTER_MAGIC` 誤套 AC -2 的問題，對齊 Java 魔法屏障只作為一次性抵消狀態並播放 `cast_gfx2`。
- 補齊 `WEAPON_BREAK` 對玩家武器損壞的 Java `Inventory.receiveDamage` 語義，損壞上限改為 `enchant + 5`，並送出訊息 268 與 `S_ItemStatus`。
- 補齊 `WEAK_ELEMENTAL` 的 Java NPC 弱點顯示行為，新增 NPC `weak_attr` 欄位並依 bitmask 廣播地/火/水/風 `S_SkillSound` 2169/2167/2166/2168。
- 補齊 `BLESSED_ARMOR` 的 Java 物品附魔計時語義，未裝備防具的鎧甲護持也會到期清除。
- 補齊 `CURSE_BLIND` 對 `STATUS_FLOATING_EYE(1012)` 目標送 `S_CurseBlind(2)` 的 Java 封包差異。
- 補齊 `TURN_UNDEAD` 的 Java NPC 目標限制，需符合 `undeadType == 1 || 3` 且 `IsTU=true` 才能即死；NPC template 與 SQL 轉檔同步新增 `undead_type` / `turn_undeadable` 欄位。
- 補齊 `DETECTION` 裝備力量魔法頭盔 20015 時 MP 消耗減半的 Java 行為。
- 補齊 `ENCHANT_WEAPON` 裝備力量魔法頭盔 20015 時 MP 消耗減半的 Java 行為。
- 補齊 `CURSE_POISON` 的 Java 毒有效目標判定，玩家目標會受毒性抵抗與生命魔眼阻擋，NPC 既有中毒時不再被覆寫。
- 補齊 `CHILL_TOUCH` / `VAMPIRIC_TOUCH` 對玩家目標的吸血回復，玩家目標傷害路徑現在會依實際套用傷害回復施法者，對齊 Java `heal = this._dmg`。
- 補齊 `CURE_POISON` 的 Java 目標特效封包，解毒立即效果完成時送出目標 `S_SkillSound` 871。
- 補齊 Java `L1SkillUse.failSkill()` 的傳送失敗解鎖，`TELEPORT`、`MASS_TELEPORT`、`TELEPORT_TO_MATHER` 前置驗證失敗時送出 TeleportUnlock。
- 修正 `TELEPORT` / `MASS_TELEPORT` 成功傳送時的封包差異，對齊 Java 不再送 generic `S_DoActionGFX` / `S_OPCODE_ACTION`，保留傳送特效與傳送流程。
- 補齊 `TELEPORT` 的 Java 傲慢之塔支配傳送符例外，3301-3310 地圖持有對應 84041-84050 或 84071 時允許單人指定與隨機傳送，且不影響 `MASS_TELEPORT`。
- 補齊 `TELEPORT` 的 Java 奪命之雷 192 狀態阻擋，有該狀態時送 normal chat 文字訊息與 TeleportUnlock，並在傳送與 MP 消耗前返回。
- 補齊 `TELEPORT` 的 Java 束縛 4000 狀態阻擋，有該狀態時送 normal chat 文字訊息並在傳送與 MP 消耗前返回。
- 補齊 `TELEPORT` / `MASS_TELEPORT` 的 Java 亡命之徒 230 狀態阻擋，有該狀態時送 1413 並在傳送與 MP 消耗前返回。
- 修正 `TELEPORT` 隨機傳送在不可順移地圖的拒絕訊息，單人隨機傳送改依 Java 使用 647，`MASS_TELEPORT` 維持 276。
- 補齊 `teleport_open` / `teleport_close` PC action，玩家可開關 `MASS_TELEPORT` 的 748 詢問流程，對齊 Java `L1ActionPc`。
- 補齊 `MASS_TELEPORT` 的 Java 預設詢問流程，同血盟成員預設收到 `S_Message_YN(748)` 並暫存傳送目的地，接受後才執行集體傳送。
- 修正 `MASS_TELEPORT` 集體傳送的 Java 成員限制，正在擺攤的同血盟成員不再被一起傳送。
- 修正 `TELEPORT` / `MASS_TELEPORT` 指定傳送的 Java 地圖限制差異：單人指定傳送依 `teleportable` 判斷並用訊息 647 拒絕，集體指定傳送依 `escapable` 判斷並用訊息 276 拒絕。
- 補齊王者加護 `DIVINE_SACRIFICE` 的 Java `BraveAvatarTimer` 行為：王族隊長已學 119 且隊伍 2 人以上時，16 格內隊員自動套用 8065 光環，離開條件後還原 STR/DEX/INT/MR 與暈眩/支撐抗性。
- 修正會心一擊 `FINAL_BURN` 的 Java 行為：HP 低於門檻時拒絕施放，成功時以施法前 MP 作為傷害，攻擊後才將 HP/MP 扣到 100/1。
- 修正力量提升 `DRESS_MIGHTY` 的取消圖示封包，移除 buff 時送出 Java `L1SkillStop` 對應的 `S_Strup` type 3。
- 修正敏捷提升 `DRESS_DEXTERITY` 的取消圖示封包，移除 buff 時送出 Java `L1SkillStop` 對應的 `S_Dexup` type 3。
- 修正迴避提升 `DRESS_EVASION` 的狀態更新封包，套用與移除 buff 時送出 Java skillmode 對應的 `S_PacketBox.UPDATE_ER(132)`。
- 修正破壞盔甲 `ARMOR_BREAK` 的成功率，對齊 yiwei 設定高等 60%、同等 40%、低等 20% 與 Java INT 加成。
- 修正三重矢 `TRIPLE_ARROW` 對玩家目標只套用一次傷害的問題，玩家目標分支現在依 Lua/Java 行為套用 3 次命中。
- 修正行走加速 `MOVING_ACCELERATION` 的二段加速互斥，施放時會移除勇敢藥水與精靈餅乾狀態，對齊 Java repeated skills 群組。

## 物品與交易

- NPC 商店購買改走共用 ItemCreate 給物品，避免商店手寫堆疊、欄位初始化與背包通知流程；購買前先做負重檢查，避免扣款後才發現給物品失敗。
- NPC 商店販賣所得 Adena 改走共用 ItemCreate 發放，讓 NPC 商店買入/賣出兩側都共用同一套物品建立與通知流程。
- 任務獎勵物品與金幣改走共用 ItemCreate 發放，保留任務完成後角色 Dirty 標記，避免任務系統重複維護堆疊與背包通知邏輯。
- 鬼屋通關獎勵 41308 改走共用 ItemCreate 發放，保留 Java 對應的 403 取得物品訊息。
- 寵物競賽獎牌 41309 改走共用 ItemCreate 發放，保留勝者公告與 403 取得物品訊息。
- 寵物馴服與進化產生的一般/高等項圈改走共用 ItemCreate 發放，並保留項圈 ObjectID 作為寵物資料關聯鍵。
- 攻城戰守城血盟禮物改走共用 ItemCreate 發放，保留取得物品訊息，避免攻城獎勵重複維護背包新增流程。
- 天寶幣商城回收給幣改走共用 ItemCreate 發放，並在扣除回收物品前確認商城幣模板可用，避免扣物後無法給幣。
- 天寶幣商城購買商品改走共用 ItemCreate 發放，並保留商城商品資料指定的強化等級。
- 新增共用 ItemCreate 給物品系統，統一處理物品模板查詢、堆疊、背包格數、負重、ChargeCount 初始化與背包封包通知。
- 強化物品商店購買商品改走共用 ItemCreate 發放，並保留預製商品的 bless、enchant 與屬性強化欄位。
- NPC 製作成品、成功額外獎勵與失敗殘留物改走共用 ItemCreate 發放，並支援製作流程在送出背包封包前套用強化與祝福欄位。
- 空白魔法卷軸寫入技能後產生的封入魔法卷軸改走共用 ItemCreate 發放，對齊 Java `Blank_MagicReel` 的 createItem/storeItem 流程。
- 寶箱開啟後發放獎勵物品改走共用 ItemCreate，並保留寶箱資料指定的 bless 與 enchant 覆寫欄位。
- 溶解劑成功溶解物品後產生的魔法結晶體改走共用 ItemCreate 發放，保留 ResolventTable 的 crystal_count 倍率計算。
- 提煉魔石成功後產生的升級魔石改走共用 ItemCreate 發放，對齊 Java Bring Stone 的 storeItem 流程。
- NPC 物品升級成功的新物品與火神精煉結晶體改走共用 ItemCreate 發放，並在消耗材料或原物品前先檢查背包格數與負重。
- 旅館退租退款與租房鑰匙改走共用 ItemCreate 發放，並新增單一物件 count 選項，保留 Java 旅館鑰匙的 InnKey 欄位與數量語義。
- 料理製作成功後改由 ItemCreate 發放結果物品，釣魚 tick 抽到魚後也改由 ItemCreate 發放獎勵，補齊原本只消耗材料/餌料但未給結果物品的缺口。
- 怪物掉落發放改走共用 ItemCreate，並保留掉落強化值與武器/防具未鑑定狀態，掉落公告與 ShowDrop/ItemBoard 通知維持原流程。
- GM 指令給物品與給金幣改走共用 ItemCreate，保留可堆疊物品忽略強化值、不可堆疊物品逐件套用 enchant 的 Java 行為。
- 進入世界時無存檔物品的初始金幣發放改走共用 ItemCreate，保留缺少共用服務時的舊版直接新增 fallback。
- 城堡資金領出發放金幣改走共用 ItemCreate，保留城堡公共資金扣除與領出訊息流程。
- 修正地面物品掉落與撿取流程，掉落時保存完整 InvItem 狀態快照，撿回背包時保留 ChargeCount、耐久、屬性強化、鑰匙欄位與未鑑定狀態，避免物品經過地面後退化成模板預設值。
- 修正玩家交易完成與取消流程，非堆疊物品保留原 ObjectID 與完整 InvItem 狀態，避免魔杖次數、耐久、屬性強化、鑰匙欄位與未鑑定狀態在交易後遺失。
- 修正交易 WAL 條目的物品識別語義，非堆疊物品轉移改記錄實例 ObjectID，堆疊物品記錄模板 item_id 與數量，recovery 分別以 `character_items.obj_id` 或 count 扣加 replay，並避免金幣 WAL 被誤當作物品轉移處理。
- 修正斷線交易清理的物品歸還流程，並補上回歸測試，確認已扣除的交易物品在斷線歸還時保留原 ObjectID 與完整 InvItem 狀態。
- 修正個人商店物品轉移流程，保留完整 InvItem 狀態與非堆疊物品原 ObjectID，避免特殊物品經由個人商店買賣後欄位遺失。
- 個人商店買入/賣出在物品與金幣轉移前寫入 `private_shop` WAL，並依非堆疊 ObjectID、堆疊 item_id/count、金幣 GoldAmount 建立可 recovery 的條目。
- 修正倉庫存入與取出流程，新增 `warehouse_items` 物品狀態欄位並在快取/取出時保留原 ObjectID、ChargeCount、耐久、屬性強化、旅館鑰匙與未鑑定狀態；部分堆疊存入會建立新的倉庫 ObjectID，避免與來源背包物品衝突。
